PROJECT(dfexec)
SET(CMAKE_STANDARD_LIBRARIES ${CMAKE_STANDARD_LIBRARIES} psapi.lib)
SET(SRC dfexec-common.c reusable_funcs.c)
IF(WIN32)
    SET(SRC ${SRC} dfexec-win32.c)
ELSE(WIN32)
    SET(SRC ${SRC} dfexec-unix.c)
ENDIF(WIN32)
ADD_LIBRARY(dfexec SHARED ${SRC})